# User Prompt
when an SMS app has unread messages, there s a small notification on the app icon showing the count of unread messages .  Do the same for this app

# Tasks: Unread Message Badging on App Icon

- [x] Add `ShortcutBadger` dependency to `app/build.gradle`
- [x] Implement `updateAppStateBadge(Context context)` in `SmsRepository.java`
- [x] Call `updateAppStateBadge(context)` in database modification methods (`markSmsAsRead`, `deleteSms`, `saveSmsToInbox`, `saveSmsToSent`) in `SmsRepository.java`
- [x] Call `updateAppStateBadge(this)` in `MainActivity.java` `onResume()` and during `loadSms()`
- [x] Call `updateAppStateBadge(context)` in `SmsReceiver.java` `onReceive()`
